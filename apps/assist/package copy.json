{
  "name": "assist-monorepo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w apps/assist",
    "build": "npm run build -ws",
    "start": "npm run start -w apps/assist",
    "lint": "npm run lint -ws",
    "typecheck": "npm run typecheck -ws"
  }
}
