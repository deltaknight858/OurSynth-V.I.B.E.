// Firebase has been removed from this project.
// This stub remains temporarily to avoid import churn while migrations complete.
// Any attempt to import from this module should be removed; if called, it will throw.

export function __deprecatedFirebaseAccess(..._args: unknown[]): never {
  throw new Error("Firebase is removed. Replace usage with your chosen provider.");
}

export const app = undefined as never;
export const auth = undefined as never;
export const db = undefined as never;
export const realTimeDb = undefined as never;
export const storage = undefined as never;
export const analytics = undefined as never;
